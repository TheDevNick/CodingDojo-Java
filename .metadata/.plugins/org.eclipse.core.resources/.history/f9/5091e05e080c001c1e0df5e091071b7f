package test.classes;

import static org.junit.Assert.assertTrue;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;


public class CharacterTests {

	public TestCharacter character;
	
	@BeforeEach
	void setup() {
		this.character = new TestCharacter("test character", 10, 15, 20, 3, "character desc.");
	}
	
	@Test
	void testGetterSetters() {
		this.character.setRace("raceTest");
		assertTrue(this.character.getRace() != null);
		
		this.character.setStrength(10);
		assertTrue(this.character.getStrength() == 15);
		
		this.character.setMaxStrength(15);
		assertTrue(this.character.getMaxStrength() == 20);
		
		this.character.setDexterity(20);
		assertTrue(this.character.getDexterity() == 25);
		
		this.character.setMagic(3);
		assertTrue(this.character.getMagic() == 5);
	}
}
