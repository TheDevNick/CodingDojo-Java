package main.java.classes.game;

import static org.junit.Assert.assertTrue;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import jdk.internal.org.jline.terminal.Size;

public class GameMapTest {
	private GameMap gameMap;

	@BeforeEach
	void setup() {
		this.gameMap = new GameMap(size);
	}

	@Test
	void testInstantiation() {
		// set the size
		assertTrue(this.gameMap.size == size);
//		instantiate grid array
		assertTrue(this.gameMap.grid.length == 10);
		assertTrue(this.gameMap.grid[0].length == 10);
		
		//center grid is correct 
		assertTrue(this.gameMap.centerGrid.x =);

	}
}
